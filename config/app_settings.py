import os
from pathlib import Path

APP_NAME = os.getenv('APP_NAME', 'My App')
APP_DESCRIPTION = os.getenv('APP_DESCRIPTION', "We're working hard to finish the development of this site. Sign up below to receive updates and to be notified when we launch!")